<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv='cache-control' content='no-cache'> 
    <meta http-equiv='expires' content='0'> 
    <meta http-equiv='pragma' content='no-cache'>
    
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <title>About Me</title>

    <div id="modeSwitchContainer">
        <div class="form-check form-switch">
            <input class="form-check-input" type="checkbox" id="modeSwitch">
            <label class="form-check-label" for="modeSwitch">
                <i id="modeIcon" class="fas fa-sun"></i>
            </label>
        </div>
    </div>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        body {
            transition: background-color 0.3s, color 0.3s;
        }

        body.bg-dark {
            background-color: #212121;
            color: #ffffff;
        }

        body.bg-light {
            background-color: #ECEDEE;
            color: #212121;
        }

        .card.bg-custom-dark {
            background-color: #2C3034 !important;
        }

        .card.bg-custom-light {
            background-color: #f8f9fa !important;
        }

        .table-dark {
            background-color: #1D2A35 !important;
        }

        .list-group-item {
            background-color: #212121 !important;
            color: white !important;
            border: 1px solid #444;
        }

        .table-light {
            background-color: #f8f9fa !important;
        }

        .list-group-item.bg-dark {
            background-color: #212121 !important;
        }

        .list-group-item.bg-light {
            background-color: #ffffff !important;
            color: #212121 !important;
        }

        .btn-outline-light,
        .btn-outline-primary {
            width: 50%;
            font-size: 18px;
            padding: 10px;
            margin-top: 10px;
        }

        .form-check-input:checked {
            background-color: #0d6efd;
            border-color: #0d6efd;
        }

        .card.bg-custom-light {
            background-color: #f8f9fa !important;
            border: 1px solid black !important;
        }

        .card {
            min-width: 300px;
        }

        #modeSwitchContainer {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(255, 255, 255, 0.8);
            padding: 10px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            z-index: 1000;
        }

        .dark-mode #modeSwitchContainer {
            background: rgba(0, 0, 0, 0.8);
        }
    </style>
</head>

<body class="bg-dark text-light">

    <div class="container text-center mt-5">
        <h1 class="fw-bold">ABOUT ME</h1>

        <h2 class="mt-4">Background</h2>
        <p>
            My name is Chen Shi, I was born in Chile and my parents are Chinese. I am currently studying Computer Engineering in Universidad de los Andes. 
            I have also studied nearly two years of Electrical Engineering and Information Technology at ZJU-UIUC but dropped due to the pandemic. 
            Now I am in the fourth year of my major.
        </p>

        <h2 class="mt-4">Skills</h2>
        <p>Here is a table showing my skillset and level of expertise:</p>

        <div class="table-responsive">
            <table class="table table-dark table-striped table-bordered">
                <thead class="table-primary">
                    <tr>
                        <th>SKILL</th>
                        <th>LEVEL</th>
                    </tr>
                </thead>
                <tbody>
                    <tr><td>Python</td><td>Advanced</td></tr>
                    <tr><td>JavaScript</td><td>Medium</td></tr>
                    <tr><td>HTML/CSS</td><td>Medium</td></tr>
                    <tr><td>Git</td><td>Basic</td></tr>
                    <tr><td>Excel</td><td>Medium</td></tr>
                    <tr><td>SQL</td><td>Medium</td></tr>
                    <tr><td>English</td><td>Advanced</td></tr>
                    <tr><td>Spanish</td><td>Native</td></tr>
                    <tr><td>Chinese</td><td>Basic</td></tr>
                    <tr><td>Django</td><td>Basic</td></tr>
                    <tr><td>C</td><td>Medium</td></tr>
                    <tr><td>C++</td><td>Medium</td></tr>
                    <tr><td>Stata</td><td>Basic</td></tr>
                    <tr><td>Assembly</td><td>Basic</td></tr>
                </tbody>
            </table>
        </div>

        <a href="https://github.com/chenshi5858" class="btn btn-outline-light mt-3">Check out my Github!</a>

        <h2 class="mt-5">Hobbies</h2>
        <p>Some of my hobbies are:</p>
        <ul class="list-group list-group-flush text-light">
            <li class="list-group-item bg-dark">Playing videogames</li>
            <li class="list-group-item bg-dark">Playing the guitar</li>
            <li class="list-group-item bg-dark">Collecting Pokemon cards</li>
            <li class="list-group-item bg-dark">Reading</li>
        </ul>

        <p class="mt-3">I recently created an Instagram account to showcase my Pokemon card collection!</p>
        

        <div class="card w-25 mx-auto mt-4 bg-custom-dark text-light border-light">
            <%= image_tag "ig_collection.png", class: "card-img-top" %>
            <div class="card-body">
                <h5 class="card-title">@oshawa_collector</h5>
                <p class="card-text">Here I post my Pokémon card collection!</p>
                <a href="https://www.instagram.com/oshawa_collector/" class="btn btn-outline-primary">Check it out!</a>
            </div>
        </div>

        <p class="mt-4">The account is named after my favorite Pokémon Oshawott, shown in the picture below.</p>

        <div class="text-center container mt-5">
            <div class="row">

                <div class="col-md-12 col-lg-7">
                    <%= image_tag "oshawott.webp", alt: "Oshawott", class: "img-fluid w-50" %>
                    <h3 id="updatedSection" class="mt-2">This is Oshawott!</h3>
                </div>

                <div class="col-md-12 col-lg-5 text-lg-start">
                    
                    <h2 class="mt-4">This Oshawott does not have a name</h2>
                    <p>Please help me name him:</p>
        
                    <div class="input-group w-50 mx-auto">
                        <input type="text" class="form-control" id="textInput" placeholder="Name this Oshawott!">
                        <button id="updateButton" class="btn btn-primary">Update</button>
                    </div>
                </div>
            </div>
        </div>
        <div class = "container text-center">
            <h2 class="col-12 mt-5">Check the latest news!</h2>
            <ul class = "mt-5 latest_news">
                
            </ul>
        </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", (event)=>{
            document.getElementById("updateButton").addEventListener("click", function() {
                var inputText = document.getElementById("textInput").value;
                document.getElementById("updatedSection").textContent = inputText || "Please name this Oshawott";
                document.getElementById("textInput").value = "";   
                
            });
            document.getElementById("textInput").addEventListener("keydown", function(event) {
                if (event.key === "Enter") {
                    event.preventDefault();
                    document.getElementById("updateButton").click();
                }
            });
        
            document.getElementById('modeSwitch').addEventListener('change', function() {
                if (this.checked) {
                    document.body.classList.remove('bg-dark', 'text-light');
                    document.body.classList.add('bg-light', 'text-dark');
        
                    let cards = document.querySelectorAll('.card');
                    cards.forEach(card => {
                        card.classList.remove('bg-custom-dark', 'text-light');
                        card.classList.add('bg-custom-light', 'text-dark');
                    });
            
                    let table = document.querySelectorAll('.table');
                    table.forEach(tbl => {
                        tbl.classList.remove('table-dark');
                        tbl.classList.add('table-light');
                    });
                    let listItems = document.querySelectorAll('.list-group-item');
                    listItems.forEach(item => {
                        item.classList.remove('bg-dark');
                        item.classList.add('bg-light');
                    });
        
                    let btnGitHub = document.querySelector(".btn-outline-light");
                    if (btnGitHub) {
                        btnGitHub.classList.remove("btn-outline-light");
                        btnGitHub.classList.add("btn-outline-dark");
                    }
                } else {
                    document.body.classList.remove('bg-light', 'text-dark');
                    document.body.classList.add('bg-dark', 'text-light');
            
                    let cards = document.querySelectorAll('.card');
                    cards.forEach(card => {
                        card.classList.remove('bg-custom-light', 'text-dark');
                        card.classList.add('bg-custom-dark', 'text-light');
                    });
            
                    let table = document.querySelectorAll('.table');
                    table.forEach(tbl => {
                        tbl.classList.remove('table-light');
                        tbl.classList.add('table-dark');
                    });
            
                    let listItems = document.querySelectorAll('.list-group-item');
                    listItems.forEach(item => {
                        item.classList.remove('bg-light');
                        item.classList.add('bg-dark');
                    });
            
                    let btnGitHub = document.querySelector(".btn-outline-dark");
                    if (btnGitHub) {
                        btnGitHub.classList.remove("btn-outline-dark");
                        btnGitHub.classList.add("btn-outline-light");
                    }
                }
            });
            
        
            getAPIContent();
        
            
        })
        
        async function getAPIContent(){
            const apiKey = "fe4a7c898fc84809a0e03c463f643127";
            const category = "technology";
        
            const url = `https://newsapi.org/v2/top-headlines?category=${category}&apiKey=${apiKey}`;
        
            await fetch(url)
                    .then(response => response.json())
                    .then(data => {
                        const topArticles = data.articles.slice(0, 10);
                        displayNews(topArticles);
                    })
                    .catch(error => console.error('Error fetching news:', error));
        }
        
        function displayNews(articles) {
            const newsList = document.querySelector('.latest_news');
            newsList.innerHTML = ''; 
        
            articles.forEach(article => {
                const listItem = document.createElement('li');
                listItem.classList.add('list-group-item', 'bg-dark', 'p-3');
                listItem.innerHTML = `
                    <h5>${article.title}</h5>
                    <p><strong>Source:</strong> ${article.source.name}</p>
                    <p>${article.description}</p>
                    <a href="${article.url}" target="_blank" class="btn btn-primary">Read more</a>
                `;
                newsList.appendChild(listItem);
            });
        }
        
    
    
    </script>

</body>
</html>
